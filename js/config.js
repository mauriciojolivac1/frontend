/**
 * URL base del backend (sin barra final).
 * Vacío = mismo origen (p. ej. detrás de un proxy que une front y API).
 * Desarrollo local con dos puertos: "http://localhost:3001"
 */
export const API_BASE = "http://localhost:3001";
